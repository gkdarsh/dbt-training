version: 2

models:
  - name: stg_orders
    description: one unique order per row
    columns:
      - name: ORDERID
        description: the PK for stg_ orders
      - name: shipmode
        description: '{{docs("shipmode")}}'